# Salt - Node-based Photo Editor

Salt is a work in progress that might one day grow into a flexible photo editor.

## Setup

1. Install dependencies
    ```sh
    pipenv install
    ```
1. Run it!
    ```sh
    python ./salt.py -h
    ```
